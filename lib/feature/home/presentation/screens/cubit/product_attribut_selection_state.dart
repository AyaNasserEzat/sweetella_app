class ProductSelectionState {
  // Holds the selections, e.g., {"Size": "M", "Flavor": "Chocolate"}
  final Map<String, String> selectedAttributes;

  ProductSelectionState({required this.selectedAttributes});
}
